class Circulo:
    #Constructor (Inicializar los atributos objeto)
    def __init__(self, radio):
        self.radio = radio #publico
        #self._radio = radio #privado (por raya_) esto se puede modificar 
        #solo atraves de un metodo
        
